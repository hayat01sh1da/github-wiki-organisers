name: Notify Unknown Wiki List

on:
  schedule:
    # 09:00 JST every Monday
    # ⚠️ The timing is somehow not accurate at all
    - cron: '0 0 * * 1'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout the Wiki Repository
        uses: actions/checkout@v4
        with:
          repository: "${{ github.repository }}.wiki"
          ref: master
          path: .wiki
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.4.5
      - name: Set ENV
        working-directory: .wiki
        run: |
          set -xe
          git pull origin master
          bash export_unknown_wiki_count_list_by_namespace.sh
          while IFS= read -r line; do
            unknown_wiki_count_list+=($line)
          done < ./unknown_wiki_count_list_by_namespace.txt
          echo UNKNOWN_BUT_NECESSARY=${unknown_wiki_count_list[@]:0:2} >> $GITHUB_ENV
          echo UNKNOWN_AND_NECESSITY_UNKNOWN=${unknown_wiki_count_list[@]:2:2} >> $GITHUB_ENV
          echo OWNERSHIP_UNDECLARED=${unknown_wiki_count_list[@]:4:5} >> $GITHUB_ENV
          rm unknown_wiki_count_list_by_namespace.txt
      - name: Slack Notification
        uses: slackapi/slack-github-action@v2
        with:
          method: chat.postMessage
          token: ${{ secrets.SLACK_APP_TOKEN }}
          payload: |
            channel: ${{ secrets.SLACK_CHANNEL_ID }}
            blocks:
              - type: "header"
                text:
                  type: "plain_text"
                  text: "【github-wiki-organisers Wiki Ownership 不明ページ件数週次レポート】"
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "<https://github.com/hayat01sh1da/github-wiki-organisers/wiki#ownerチームが不明だが必要なページ群|${{ env.UNKNOWN_BUT_NECESSARY }}>"
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "<https://github.com/hayat01sh1da/github-wiki-organisers/wiki#ownerチーム要or不要が不明なページ群|${{ env.UNKNOWN_AND_NECESSITY_UNKNOWN }}>"
              - type: "section"
                text:
                  type: "mrkdwn"
                  text: "<https://github.com/hayat01sh1da/github-wiki-organisers/wiki#owner記名なし|${{ env.OWNERSHIP_UNDECLARED }}>"
